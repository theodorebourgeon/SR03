 
CREATE TABLE THEME (
	themeId INTEGER AUTO_INCREMENT PRIMARY KEY,
	title VARCHAR(255) NOT NULL
	);

DELETE FROM SURVEY;

ALTER TABLE SURVEY
    	ADD COLUMN themeId INTEGER AFTER surveyId;

ALTER TABLE SURVEY
	ADD CONSTRAINT FOREIGN KEY(themeId) REFERENCES THEME(themeId);

ALTER TABLE SURVEY
	MODIFY themeId INTEGER NOT NULL;